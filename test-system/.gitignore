# Ігноруємо папку збірки Maven (дуже важливо, вона важка і генерується автоматично)
target/

# Ігноруємо налаштування IDE (щоб не передавати особисті налаштування комп'ютера)
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# Ігноруємо системні файли Mac/Windows
.DS_Store
Thumbs.db

# Ігноруємо папку з завантаженими файлами (щоб не викладати файли користувачів в інтернет)
uploads/

# Ігноруємо файли з паролями та секретами (ДУЖЕ ВАЖЛИВО ДЛЯ БЕЗПЕКИ!)
.env
application-local.properties
